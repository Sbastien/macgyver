# =============================================================================
# experimental — tried once, kept just in case, or heavy one-off dependencies.
#
# Not part of the root Brewfile. Install on top of base with:
#
#   mise run install:experimental
#
# This profile exists so that base stays honest. Anything here is a candidate
# for deletion at the next review rather than something to carry forever.
# =============================================================================

# Document Toolchain
# @icon:puzzle
# All six are standalone: `brew uses --installed` reports nothing depending on
# any of them, so they were installed deliberately rather than pulled in.
# Whether they are still needed is the open question.
brew "pandoc"               # Document converter
brew "weasyprint"           # HTML to PDF
brew "ghostscript"          # PostScript / PDF interpreter
brew "poppler"              # PDF utilities
cask "basictex"             # Minimal TeX distribution

# Misc
# @icon:code
brew "figlet"               # ASCII art banners
